// B-S-1 controller script v0.1.0

LOCK STEERING TO HEADING(90, 90).
STAGE.
UNTIL (STAGE:NUMBER > 0) OR (ALTITUDE > 70000)
{
	MNV_BURNOUT(true).
}

WAIT UNTIL (VELOCITY < 250) OR (ALTITUDE < 1000).
STAGE. 
